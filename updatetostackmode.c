#include "monty.h"
/**
 * updatetostackmode - stack mode
 * @head: first item
 * @ln: line number
 * Return: null
 */
void updatetostackmode(stack1_t **head, unsigned int ln)
{

	(void)head;
	(void)ln;
	variables.mode = "stack";
}
